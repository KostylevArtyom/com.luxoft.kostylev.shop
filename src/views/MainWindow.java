package views;

import javax.swing.*;

public class MainWindow extends JFrame {
    public MainWindow(){
        super("Shop");
        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setSize(1000, 800);
    }
}